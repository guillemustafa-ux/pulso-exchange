[pytest]
asyncio_mode = auto
testpaths = tests
# `pytest` invocado directo (CI) no agrega el cwd a sys.path como sí lo hace
# `python -m pytest` — sin esto, `from app import ...` falla en Actions.
pythonpath = .
